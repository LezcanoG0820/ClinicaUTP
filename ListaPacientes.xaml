<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels ="clr-namespace:ClinicaUTP.ViewModels" 
             x:DataType="viewmodels:PacientesViewModel"
             x:Class="ClinicaUTP.ListaPacientes"
             Title="Bienvenido a la Clinica Universitaria">
    <VerticalStackLayout>
        <Button Text="Agregar Paciente" Command="{Binding AddUpdatePacienteCommand}" HorizontalOptions="EndAndExpand"/>
        <Label 
            Text="Lista de Pacientes"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
    </VerticalStackLayout>
</ContentPage>